# I PAVIMENTI sono le versioni su cui la suite gira DAVVERO, non le piu'
# vecchie che forse funzionerebbero.
#
# Un pavimento sotto la versione provata e' un'affermazione di supporto che
# niente verifica -- la stessa classe di difetto che questo progetto insegue
# ovunque. Dichiarare `anthropic>=0.87.0` mentre si prova solo su 1.0.0
# significa promettere decine di versioni che nessuno ha mai eseguito.
#
# I TETTI restano al major successivo: quelli sono una difesa contro un
# cambiamento che romperebbe, e non hanno bisogno di essere provati per valere.
#
# Le PR di dependabot che proponevano pavimenti piu' bassi di questi sono
# superate: le loro versioni erano gia' vecchie quando sono arrivate.
aiohttp>=3.14.3,<4.0.0
anthropic>=1.0.0,<2.0.0
apscheduler>=3.11.3,<4.0.0
python-dotenv>=1.2.2
pytest>=9.1.1,<10.0.0
pytest-asyncio>=1.4.0,<2.0.0
pytest-aiohttp>=1.1.1,<2.0.0
httpx>=0.28.1,<1.0.0
openai>=3.1.0,<4.0.0
model2vec>=0.9.0
pyyaml>=6.0.3,<7.0

# Il linter e' parte del cancello, non un accessorio: sta qui perche' il CI lo
# installa con tutto il resto. Il TETTO al minor successivo e' la difesa vera —
# ruff cambia il proprio set di default fra minor, e senza tetto un CI verde
# diventerebbe rosso senza che nessuno abbia toccato una riga.
ruff>=0.16.4,<0.17.0
