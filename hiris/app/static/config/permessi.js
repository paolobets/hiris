/* HIRIS · Designer · permessi (tools, actions)
   SP-4 Fase B Task 3: buildToolChecks/buildActionChecks/getSelectedTools/
   getSelectedActions/updateServicesVisibility sono stati assorbiti in
   config/editor-kit.js (HirisEditorKit.checkGroup) — istanza-scoped, non
   più #tool-checks/#action-checks globali. Vedi chatbot-editor.js
   populatePermessi() per l'uso.
   Il selettore entità era già stato estratto in config/entity-picker.js
   (istanziabile — HirisEntityPicker.create()) nel Task 1.
   File mantenuto (vuoto) solo per l'ordine di caricamento/cache-busting
   per-file in config.html — nessuna logica propria residua. */
