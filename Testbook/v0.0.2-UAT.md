# UAT — HIRIS v0.0.2

**Release:** v0.0.2  
**Data:** 2026-04-18  
**Testato da:** _______________  
**Data test:** _______________

---

## Riepilogo

| Categoria | Totale | ✅ PASS | ❌ FAIL | ⏭ SKIP |
|-----------|--------|---------|---------|---------|
| Infrastruttura | 3 | | | |
| Chat NL | 4 | | | |
| Agent Designer | 5 | | | |
| Agenti — Trigger | 3 | | | |
| Notifiche | 3 | | | |
| Strumenti Claude | 3 | | | |
| Sicurezza | 1 | | | |
| **TOTALE** | **22** | | | |

**Esito complessivo:** ⬜ PENDING

---

## Cosa è stato implementato in v0.0.2

- **HA Client** — REST + History API + WebSocket (state_changed)
- **8 strumenti Claude** — entity states, energy history, weather forecast, service call, notifiche, automazioni
- **Claude Runner** — agentic loop multi-turn con tool use (AsyncAnthropic)
- **AgentEngine** — scheduler APScheduler, trigger state_changed, CRUD agenti
- **API REST** — `/api/chat`, `/api/agents` (CRUD), `/api/status`, `/api/health`
- **Chat UI** (`/`) — interfaccia NL dark theme, storico conversazione, sidebar agenti
- **Agent Designer** (`/config`) — form step-by-step, trigger condizionali, 8 checkbox strumenti
- **32 test automatici** — copertura ha_client, tools, claude_runner, agent_engine, API

---

## TC-INF — Infrastruttura

### TC-INF-01 🔴 — Avvio add-on e health check

**Prerequisiti:** Add-on installato su Home Assistant, `CLAUDE_API_KEY` configurata nelle opzioni

**Passi:**
1. Avvia l'add-on dalla pagina Supervisor
2. Apri i log dell'add-on
3. Chiama `GET http://<ha-ip>:8099/api/health`

**Atteso:**
- Nessun errore nei log all'avvio
- Risposta: `{"status": "ok", "version": "0.0.2"}`

**Status:** ⬜ PENDING

**Issues:**
- _nessuna_

---

### TC-INF-02 🔴 — Status endpoint con agenti

**Prerequisiti:** TC-INF-01 PASS, almeno un agente creato (vedi TC-DES-01)

**Passi:**
1. Chiama `GET /api/status`

**Atteso:**
```json
{
  "version": "0.0.2",
  "agents": {
    "total": 1,
    "enabled": 1
  }
}
```

**Status:** ⬜ PENDING

**Issues:**
- _nessuna_

---

### TC-INF-03 🔴 — Avvio senza CLAUDE_API_KEY

**Prerequisiti:** Add-on avviato senza `CLAUDE_API_KEY` nelle opzioni

**Passi:**
1. Avvia l'add-on senza impostare `CLAUDE_API_KEY`
2. Chiama `GET /api/health`
3. Chiama `POST /api/chat` con `{"message": "ciao"}`

**Atteso:**
- Health: `{"status": "ok", "version": "0.0.2"}` (l'add-on parte comunque)
- Chat: risposta `503 Service Unavailable` con messaggio di errore

**Status:** ⬜ PENDING

**Issues:**
- _nessuna_

---

## TC-CHAT — Chat NL

### TC-CHAT-01 🔴 — Chat UI si carica

**Prerequisiti:** TC-INF-01 PASS

**Passi:**
1. Apri l'add-on tramite Ingress da HA (o naviga a `http://<ha-ip>:8099/`)

**Atteso:**
- Pagina chat con tema scuro
- Sidebar sinistra con lista agenti (vuota se nessun agente creato)
- Campo input messaggio in basso
- Nessun errore console del browser

**Status:** ⬜ PENDING

**Issues:**
- _nessuna_

---

### TC-CHAT-02 🔴 — Invio messaggio e risposta Claude

**Prerequisiti:** TC-CHAT-01 PASS, `CLAUDE_API_KEY` valida

**Passi:**
1. Scrivi "quali luci sono accese in casa?" e invia
2. Attendi la risposta

**Atteso:**
- Claude risponde in linguaggio naturale
- La risposta riflette lo stato attuale delle entità HA (tool `get_entity_states` invocato)
- Il messaggio e la risposta appaiono nella conversazione

**Status:** ⬜ PENDING

**Issues:**
- _nessuna_

---

### TC-CHAT-03 🟡 — Multi-turn conversation

**Prerequisiti:** TC-CHAT-02 PASS

**Passi:**
1. Chiedi "che temperatura fa fuori?"
2. Senza ricaricare, chiedi "e rispetto a ieri?"

**Atteso:**
- Il secondo messaggio tiene conto del contesto (Claude usa la history)
- Risposta coerente con la domanda di follow-up

**Status:** ⬜ PENDING

**Issues:**
- _nessuna_

---

### TC-CHAT-04 🟡 — Storico conversazione (scrollback)

**Prerequisiti:** TC-CHAT-02 PASS

**Passi:**
1. Invia almeno 5 messaggi/risposte
2. Fai scroll up nella chat

**Atteso:**
- Tutti i messaggi precedenti visibili (fino a 40)
- Scroll fluido

**Status:** ⬜ PENDING

**Issues:**
- _nessuna_

---

## TC-DES — Agent Designer

### TC-DES-01 🔴 — Agent Designer si carica

**Prerequisiti:** TC-INF-01 PASS

**Passi:**
1. Naviga a `http://<ha-ip>:8099/config`

**Atteso:**
- Pagina Agent Designer caricata
- Lista agenti (vuota)
- Pulsante "New Agent"
- Nessun errore console

**Status:** ⬜ PENDING

**Issues:**
- _nessuna_

---

### TC-DES-02 🔴 — Creazione agente Proactive Monitor

**Prerequisiti:** TC-DES-01 PASS

**Passi:**
1. Clicca "New Agent"
2. Compila:
   - Name: `Test Monitor`
   - Type: `Proactive Monitor`
   - Trigger: `5` (minuti)
   - System prompt: `Sei un assistente per la casa. Monitora le entità e avvisa se qualcosa è anomalo.`
   - Tools: seleziona `get_entity_states` e `send_notification`
3. Clicca "Save"

**Atteso:**
- Agente salvato senza errori
- Agente appare nella lista a sinistra
- Status dot verde (enabled)

**Status:** ⬜ PENDING

**Issues:**
- _nessuna_

---

### TC-DES-03 🔴 — Trigger condizionali nel form

**Prerequisiti:** TC-DES-01 PASS

**Passi:**
1. Seleziona Type = `Proactive Monitor` → verifica che appaia il campo "Interval (minutes)"
2. Seleziona Type = `Reactive Agent` → verifica che appaia il campo "Entity ID to watch"
3. Seleziona Type = `Preventive Scheduler` → verifica che appaia il campo "Cron expression"
4. Seleziona Type = `Chat NL Agent` → verifica che non appaiano campi trigger

**Atteso:** I campi trigger cambiano dinamicamente in base al tipo selezionato

**Status:** ⬜ PENDING

**Issues:**
- _nessuna_

---

### TC-DES-04 🟡 — Test Run agente

**Prerequisiti:** TC-DES-02 PASS, `CLAUDE_API_KEY` valida

**Passi:**
1. Seleziona l'agente "Test Monitor" nella lista
2. Clicca "Test Run"

**Atteso:**
- L'agente viene eseguito immediatamente
- Compare un feedback di esito (successo o errore)

**Status:** ⬜ PENDING

**Issues:**
- _nessuna_

---

### TC-DES-05 🟡 — Eliminazione agente

**Prerequisiti:** TC-DES-02 PASS

**Passi:**
1. Seleziona l'agente "Test Monitor"
2. Clicca "Delete"
3. Conferma l'eliminazione

**Atteso:**
- Agente rimosso dalla lista
- `GET /api/agents` non restituisce più l'agente
- Scheduled job rimosso (il monitor non si avvia più)

**Status:** ⬜ PENDING

**Issues:**
- _nessuna_

---

## TC-TRG — Trigger agenti

### TC-TRG-01 🟡 — Agente interval si avvia automaticamente

**Prerequisiti:** TC-DES-02 PASS, `CLAUDE_API_KEY` valida

**Passi:**
1. Crea un agente Proactive Monitor con interval = `1` minuto
2. Attendi almeno 2 minuti
3. Controlla i log dell'add-on

**Atteso:**
- Nei log compaiono esecuzioni dell'agente ogni ~1 minuto
- Nessun errore di scheduling

**Status:** ⬜ PENDING

**Issues:**
- _nessuna_

---

### TC-TRG-02 🟡 — Agente cron si avvia all'orario previsto

**Prerequisiti:** `CLAUDE_API_KEY` valida

**Passi:**
1. Crea un agente Preventive Scheduler con cron expression corrispondente a ~2 minuti nel futuro (es. `30 14 * * *` per le 14:30)
2. Attendi l'orario
3. Controlla i log

**Atteso:**
- L'agente si avvia all'orario specificato dal cron
- Nessun errore di parsing del cron

**Status:** ⬜ PENDING

**Issues:**
- _nessuna_

---

### TC-TRG-03 🟡 — Agente reattivo su state_changed

**Prerequisiti:** `CLAUDE_API_KEY` valida, WebSocket HA funzionante

**Passi:**
1. Crea un agente Reactive Agent con Entity ID = `light.living_room` (o altra entità presente)
2. Cambia lo stato dell'entità da HA (accendi/spegni la luce)
3. Controlla i log dell'add-on

**Atteso:**
- Entro pochi secondi dal cambio stato, l'agente si avvia
- Nei log compare l'esecuzione reattiva

**Status:** ⬜ PENDING

**Issues:**
- _nessuna_

---

## TC-NOT — Notifiche

### TC-NOT-01 🟡 — Notifica HA push

**Prerequisiti:** `HA_NOTIFY_SERVICE` configurato (es. `notify.mobile_app_iphone`), app HA su mobile

**Passi:**
1. Chiedi via chat: "mandami una notifica di test su HA push"
2. (o crea un agente con `send_notification` configurato su `ha_push`)

**Atteso:**
- Notifica ricevuta sull'app mobile HA

**Status:** ⬜ PENDING

**Issues:**
- _nessuna_

---

### TC-NOT-02 🟢 — Notifica Telegram

**Prerequisiti:** `TELEGRAM_TOKEN` e `TELEGRAM_CHAT_ID` configurati nelle opzioni add-on

**Passi:**
1. Chiedi via chat: "mandami un messaggio Telegram di test"

**Atteso:**
- Messaggio ricevuto su Telegram con il testo richiesto

**Status:** ⬜ PENDING

**Issues:**
- _nessuna_

---

### TC-NOT-03 🟢 — Notifica Retro Panel

**Prerequisiti:** Retro Panel in esecuzione su `http://retropanel:8098` (o URL configurato)

**Passi:**
1. Chiedi via chat: "invia una notifica al pannello kiosk"

**Atteso:**
- Toast di notifica visibile su Retro Panel

**Status:** ⬜ PENDING

**Issues:**
- _nessuna_

---

## TC-TOOLS — Strumenti Claude

### TC-TOOLS-01 🔴 — Weather forecast

**Prerequisiti:** TC-CHAT-02 PASS, connessione internet

**Passi:**
1. Chiedi via chat: "che tempo farà nelle prossime 6 ore?"

**Atteso:**
- Claude chiama `get_weather_forecast` (visibile nei log se debug abilitato)
- Risposta include temperatura, copertura nuvolosa e precipitazioni
- Coordinate corrispondono alla posizione HA configurata

**Status:** ⬜ PENDING

**Issues:**
- _nessuna_

---

### TC-TOOLS-02 🟡 — Energy history

**Prerequisiti:** TC-CHAT-02 PASS, sensori energia configurati in HA

**Passi:**
1. Chiedi via chat: "mostrami i consumi energetici di oggi"

**Atteso:**
- Claude chiama `get_energy_history`
- Risposta con dati di consumption, solar production, grid import/export
- Se i sensori non esistono in HA, Claude lo comunica chiaramente

**Status:** ⬜ PENDING

**Issues:**
- _nessuna_

---

### TC-TOOLS-03 🟡 — Toggle automazione

**Prerequisiti:** TC-CHAT-02 PASS, almeno un'automazione presente in HA

**Passi:**
1. Chiedi via chat: "disabilita l'automazione [nome automazione]"
2. Verifica in HA → Automazioni che sia effettivamente disabilitata
3. Chiedi via chat: "riabilitala"
4. Verifica che sia riabilitata

**Atteso:**
- Claude chiama `toggle_automation` correttamente
- Stato automazione cambia in HA

**Status:** ⬜ PENDING

**Issues:**
- _nessuna_

---

## TC-SEC — Sicurezza

### TC-SEC-01 🔴 — CLAUDE_API_KEY non esposta al browser

**Prerequisiti:** TC-CHAT-02 PASS

**Passi:**
1. Apri DevTools del browser (F12) → tab Network
2. Invia un messaggio in chat
3. Ispeziona tutte le request/response verso `/api/chat` e `/api/status`
4. Cerca "sk-ant" o "CLAUDE" in tutte le risposte

**Atteso:**
- La chiave API non appare mai nel body di nessuna risposta HTTP
- La chiave non è presente nel codice sorgente delle pagine HTML

**Status:** ⬜ PENDING

**Issues:**
- _nessuna_

---

## Criteri di approvazione v0.0.2

Il rilascio è **APPROVATO** se:
- Tutti i test 🔴 sono `✅ PASS`
- Nessun issue critico aperto senza workaround

Il rilascio è **CONDIZIONALMENTE APPROVATO** se:
- Tutti i test 🔴 sono `✅ PASS`
- Alcuni test 🟡 sono `❌ FAIL` con issue aperta e workaround documentato

Il rilascio è **BLOCCATO** se:
- Almeno un test 🔴 è `❌ FAIL`

---

## Issues v0.0.2

| ID | Caso test | Descrizione | Priorità | Stato | Note |
|----|-----------|-------------|----------|-------|------|
| — | — | — | — | — | _aperte durante i test_ |
